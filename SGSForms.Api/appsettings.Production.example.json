{
  // Copy to appsettings.Production.json (never commit it) and fill in, or set the environment variables
  // ConnectionStrings__Default and Jwt__Key instead.
  // Generate a key (PowerShell):  [Convert]::ToBase64String([Security.Cryptography.RandomNumberGenerator]::GetBytes(48))
  "ConnectionStrings": {
    "Default": "server=localhost;port=3306;database=sgs_forms_db;user=sgs_app;password=CHANGE_ME;TreatTinyAsBoolean=false"
  },
  "Jwt": {
    "Key": "CHANGE_ME_TO_A_RANDOM_KEY_OF_AT_LEAST_32_CHARACTERS"
  }
}
